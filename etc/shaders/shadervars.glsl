flat vec3 color;
vec3 color_smooth;
vec3 normal;
vec3 position;
vec4 shadow_coord;
vec3 frag_pos;

// out flat vec3 v_color;
// out vec3 v_color_smooth;
// out vec3 v_ray;
// out vec3 v_normal;
// out vec3 v_position;
// out vec4 v_shadow_coord;
// out vec3 v_frag_pos;
